<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /studybuddy/
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /studybuddy/index.html [L]
</IfModule>
